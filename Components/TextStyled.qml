import QtQuick

import qs.Constants

Text {
    color: Colors.foreground
    elide: Text.ElideRight
    antialiasing: true
    font {
        pixelSize: Styles.textMd
        family: Styles.defaultFontFamily
        bold: true
    }
}
